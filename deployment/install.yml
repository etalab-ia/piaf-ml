---
- hosts: sandbox
  tasks:
    - name: Install haystack
      include_role:
        name: 'haystack'
      loop: "{{ deployments.keys() }}"
      loop_control:
        loop_var: client
  roles:
    - { role: nginx, tags: ["install"]}
